HOW TO INSTALL:

Start MySql Shell and run:

1) create betagaming; --> Create a new database named betagaming
2) use betagaming;    --> Switch to that database
3) source C:\Generator.txt; --> Run the script on that database which will create the tables.